// Find area of Square formula : a=a2(Square);
 #include<stdio.h>
 int main(){
float area,a;

        printf("Enter the lengh of squares: ");
        scanf("%f",&a);

        area=a*a;

        printf("\nArea of square = %.2f", area);
        
        return 0;

 }